import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.barikwrapped.barikwrapped',
  appName: 'Barik Wrapped',
  webDir: 'build',
  ios: {
    contentInset: 'always',
  },
};

export default config;
